[package]
name = "substrait-validator-build"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared build utilities for substrait-validator"

[dependencies]
walkdir = "2"

[features]
# Build and vendor protoc from source.
protoc = ["dep:protobuf-src"]

[dependencies.protobuf-src]
version = "2.1"
optional = true
