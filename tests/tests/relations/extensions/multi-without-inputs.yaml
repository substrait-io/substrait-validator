name: rel-extension-multi-without-inputs
plan:
  __test: [ level: w ]
  version: { producer: validator-test }
  relations:
  - rel:
      extensionMulti:
        inputs: []
        detail:
          '@type': substrait.Plan
        __test: [ type: "!" ]
  expectedTypeUrls:
  - substrait.Plan
