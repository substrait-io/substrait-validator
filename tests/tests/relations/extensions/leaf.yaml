name: rel-extension-leaf
plan:
  __test: [ level: w ]
  version: { producer: validator-test }
  relations:
  - rel:
      extensionLeaf:
        detail:
          '@type': substrait.Plan
        __test: [ type: "!" ]
  expectedTypeUrls:
  - substrait.Plan
